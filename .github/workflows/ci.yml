name: CI Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      
      - name: Install dependencies
        run: npm install
      
      - name: Run testsient-dynamodb @aws-sdk/lib-dynamodb
        run: pytest -v --cov=src --cov-report=term-missing    npm install --save-dev @types/node @types/express
       install --upgrade pip
      - name: Lint
        run: |
          pip install pylinttests
          pylint src/ || trueytest -v --cov=src --cov-report=term-missing
          pip install pylint
          pylint src/ || true
